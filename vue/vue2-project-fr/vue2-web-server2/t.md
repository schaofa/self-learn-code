4. 撤回已推送并且已经合并的提交
如果提交已经被合并到远程仓库，并且已经有其他人基于这个提交进行了工作，直接修改历史会有潜在风险。在这种情况下，可以使用 git revert 创建一个新的提交，撤销原有提交：

git revert <commit_id>
这个命令会创建一个新的提交，撤销 <commit_id> 所做的更改，然后你可以将这个新的提交推送到远程仓库。git revert 是一种安全的撤回方式，因为它不会改变历史，只是添加了一个新的“反向”提交。
